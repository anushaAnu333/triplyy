{
  "name": "triply",
  "private": true,
  "description": "TRIPLY – frontend and backend",
  "scripts": {
    "test": "npm run test --prefix triply-backend && npm run test:e2e --prefix triply-frontend",
    "test:backend": "npm run test --prefix triply-backend",
    "test:frontend": "npm run test:e2e --prefix triply-frontend"
  }
}
